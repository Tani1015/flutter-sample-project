import 'package:flutter/material.dart';

class Homework extends StatelessWidget {

  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("課題"),
      ),
      body: Center(child: Text("構想中!!")
      ),
    );
  }
}